# 變更紀錄

本文件記錄聲筆 Wa-Gong for macOS 各公開版本中，使用者可感知的重要變更。

## 尚未發布

### 新增

- 錄音自動清理新增容量上限，預設啟用並同時限制為 3 天與 300 MB；任一限制達到清理條件時從最舊錄音開始清除，保留轉錄文字與整理結果。保留使用者已明確選擇的舊設定。
- 歷史設定顯示錄音用量，可同時調整保留天數與容量上限；正在轉錄或重新轉錄的音檔不會被清理。
- 新增內建鍵盤與 USB 鍵盤專用快速鍵，同一個動作可依鍵盤使用不同按鍵配置。
- 新增鍵盤選擇、未連線狀態、裝置專用快速鍵編輯與刪除介面。
- 新增輸入監控權限說明、系統設定導引，以及權限與鍵盤監聽診斷資訊。
- 新增藍牙鍵盤的本次連線驗證流程，只有選定鍵盤實際送出按鍵事件後，才能錄製裝置專用快速鍵。

### 變更

- 錄音啟動時先顯示「正在連接麥克風」，收到持續音訊並成功寫入後才顯示錄音狀態與播放提示音；等待逾時會顯示中文錯誤。實際 iPhone 麥克風仍需完成硬體驗收。
- 文字整理預設等待時間由 7 秒改為 20 秒，預設不在逾時後重送。已自行啟用重試時，顯示中文重試次數；移除與底層網路套件重疊的網路錯誤重試。
- 音檔清理改為 App 啟動、每 5 分鐘及轉錄完成後檢查，關閉主視窗後仍會運作。只清除 App 錄音目錄內已關聯至處理結束紀錄的音檔，不刪除手動匯入來源或符號連結。
- 修正新增藍牙鍵盤專用快速鍵時，鍵盤監聽可能在驗證完成前中途停止，導致按下字母鍵仍無法完成驗證的問題。
- 修正鍵盤監聽可列出裝置但收不到按鍵輸入的問題；驗證時若按鍵來自另一把鍵盤或不同連線方式，會顯示實際偵測到的鍵盤與連線方式，方便切換後重試。
- 修正藍牙鍵盤連線驗證成功後，按下「錄製」會立即中斷，無法設定裝置專用快速鍵的問題。
- 既有快速鍵會自動遷移為「所有鍵盤」，不需要重新設定，也不會因升級要求輸入監控權限。
- 重新錄製快速鍵時，只有成功錄製並通過衝突驗證後才會取代舊設定；取消錄製會保留原設定。
- 所有可設定的快速鍵都可個別清除；清除後不會因預設顯示值而重新出現。
- 從本機 Build 升級為正式版時，既有的雲端服務 API Key 會安全搬移至正式版 Keychain；確認新位置寫入成功後，才會移除舊項目。
- 本機測試版若沒有個別覆寫，會唯讀沿用正式版 Keychain 中的雲端服務 API Key，測試版新增或修改金鑰仍只寫入本機測試區。
- 外部 AI 傳送提示會跟隨迷你錄音浮窗的位置，上方配置顯示於浮窗下方，下方配置顯示於浮窗上方。
- 外部 AI 傳送提示會依 App 語言顯示標題、欄位名稱與資料類型，不再固定使用英文。

### 隱私

- 裝置專用快速鍵只在本機記憶體中即時比對，不保存或傳送一般按鍵內容、按鍵歷史與原始裝置序號。

### 已知限制

- 藍牙鍵盤的休眠、喚醒與重新連線仍需依實際硬體完成相容性測試；重新連線後若要修改設定，必須重新驗證本次連線。
- 沒有可用序號的相同型號鍵盤無法逐把辨識，裝置專用設定會保守套用到所有相同型號鍵盤。
- 來源事件無法在短時間內唯一配對時，Wa-Gong 不會猜測來源，也不會觸發該次裝置專用快速鍵。

## 1.0.0（2026-08-28）

### 新增

- 完成 Wa-Gong 品牌、Bundle ID、App 圖示與繁體中文介面遷移。
- 加入 OpenAI 雲端語音轉錄，支援 `gpt-4o-mini-transcribe`、`gpt-4o-transcribe`、`gpt-transcribe`、`gpt-4o-transcribe-diarize` 與 `whisper-1`。
- 加入 Gemini 語音轉錄與文字整理流程，支援台灣繁體中文輸出規則。
- 新增提示詞管理介面，可檢視、建立與編輯提示詞，並避免重複的內建提示詞。
- 新增外部 Request 隱私提示，顯示目的地、模型與即將傳送的資料類型。
- 新增迷你錄音浮窗的上方與下方位置設定，預設顯示於螢幕上方中央。

### 變更

- 新手引導與預設模式改為雲端語音轉錄優先，不要求先下載本機模型。
- OpenAI 語音轉錄預設使用 `gpt-4o-mini-transcribe`。
- 內建文字整理提示詞改以台灣慣用語、保留來源語言與精簡處理為原則。
- 錄音浮窗沿用 App 語言設定，繁體中文狀態顯示為「正在轉錄」與「正在潤飾」。
- 錄音器樣式名稱改為「迷你浮窗」與「上方瀏海」。

### 隱私

- 雲端語音轉錄只會將錄音送至使用者選定的轉錄服務。
- 本機語音轉錄不會將錄音送至雲端轉錄服務；若模式啟用雲端 AI 潤飾，轉錄文字仍會送至所選 AI Provider。
- 剪貼簿內容不會傳送給 AI Provider。
- 日誌不記錄完整錄音、完整轉錄文字、剪貼簿內容或 API Key。

### 已知限制

- 使用 OpenAI、Gemini 或其他雲端 Provider 時，使用者必須自行提供有效的 API Key，相關費用由服務供應商計收。
- 本機 Build 不包含 iCloud 詞典同步與自動更新。
